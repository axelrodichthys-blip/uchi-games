# HANDOFF.md — 引き継ぎメモ

最終更新: 2026-09-18（クラウドセッション。本キャラをゲームに入れ、**不具合 3 件を直し、飛行を追加**）

## ブラウザで確認できる URL

- **https://axelrodichthys-blip.github.io/uchi-games/** （main に push すると 1〜2 分で自動更新される）
  - 起動すると **「雨の景色」** から始まる。正面の遠くに灯りがあり、飛び石の道と消えた街灯の列が続く
  - **灯りに触れると「ネオン」へ移る。** ネオンの道の突き当たりの光るゲートに触れると雨の景色へ戻る
  - **F2**（スマホは「ワールド」ボタン）でワールドを順に切替: 雨の景色 → ネオン → 灰色の世界（検証用）
  - PC: 右ボタンを押しながらマウスで視点（Tab で固定 / 解除）、WASD 移動、Shift で走る、Space でジャンプ、ホイールで距離、V で一人称 / 三人称、F1 で調整パネル
  - **飛行: F キー（パッドは Y）で飛び立つ / 降りる。** 飛行中は WASD で移動、**Space で上昇、Ctrl か C で下降、Shift で加速**。
    地面に着けば自動で解除。スマホは右上の「飛ぶ」ボタン、右下の「降りる」ボタン
  - スマホ（Chrome）: 左下をなぞると仮想スティック、右側をなぞると視点、2 本指でカメラ距離。右下に「ジャンプ」「走る」、右上に「視点」「調整」「ワールド」
  - コントローラー: 左スティック移動、右スティック視点、LB で走る、A でジャンプ、Start で視点切替

> **2026-09-18 更新: 本キャラがゲームに入りました。** 上の URL で歩けるのは**とんがり帽子とポンチョの旅人**です。
> Mixamo のリグはまだなので、既存リグを本キャラの寸法に合わせ直した**暫定版**です（動きは今までと同じ 10 本）。

## 今回（2026-09-18）やったこと — 本キャラのモデルを組み立てた

### 見てほしい画像（GitHub 上でそのまま開けます）
- `docs/reference/character/traveler_model_compare_poncho.png` … **上が基準画像 / 下が作ったモデル**（ポンチョ姿の四面図）
- `docs/reference/character/traveler_model_compare_body.png` … 同じくポンチョ無しの姿

### 1. 基準画像を「測って」から作った
確定ターンアラウンド 2 枚は別々に生成したもので、**寸法が揃っていませんでした**（ポンチョ無しのほうが脚も胴も長い）。
そこで `tools/measure_reference.py` を作って画素で測り直したところ、
**目の帯（立ち襟の上〜つばの下）だけは 2 枚でぴたり一致する**ことが分かったので、そこを重ねて縮尺を決めました。

- 見えるシルエット（ブーツ・ポンチョの裾・つば・帽子）→ **ポンチョ姿の画**から
- 中の衣装（ベスト・ベルト・半ズボン・手袋・鞄）→ **ポンチョ無しの画**から

**設計書に最初に書いていた高さ表（つば 1.28m など）は手描きスケッチ基準の目分量で、確定画像とは合っていませんでした。**
実測値に置き換えました（表は `docs/reference/character/README.md`）。主なもの:

| 部位 | 旧（目分量） | 新（実測） |
|---|---|---|
| ブーツの上端 | 0.18 m | **0.216 m** |
| ポンチョの裾 | 0.34 m | **0.365 m** |
| 立ち襟の上 = 目の帯の下 | 1.18 m | **1.128 m** |
| つば = 目の帯の上 | 1.28 m | **1.200 m** |
| 帽子の円錐の根元 | 1.30 m | **1.205 m** |
| 素体（帽子なし）の頭のてっぺん | — | **1.300 m** |

### 2. `tools/blender/build_traveler.py`（新規）でメッシュを組み立てた
1 回流すと 4 つ出ます:

| 出力 | 使い道 |
|---|---|
| `docs/reference/character/traveler_body.obj` | **Mixamo にアップロードするのはこれ**（ポンチョ・帽子なし、A ポーズ） |
| `docs/reference/character/traveler_body_height.txt` | 上の高さ（1.3156 m、羽の突起の先まで）。変換時に渡す |
| `game/assets/traveler_body.glb` | 素体の確認用 |
| `game/assets/traveler_outfit.glb` | ポンチョ + 帽子。リグの後に被せる |
| `game/assets/traveler_preview.glb` | 完成形の確認用（ゲームでは読まない） |

入っているもの: とんがり帽子（後ろへ反る円錐 + 前下がりの反ったつば + 丸ボタン）、
立ち襟のポンチョ（前の重ね合わせ + 胸の丸ボタン + 波打つ裾）、卵型の頭と縦長の目、細い首、
頭頂部の羽の突起、袖なしベスト（折り襟 + ボタン 3 つ）、ベルトとバックル、
折り返しの半ズボン、腰の後ろの鞄、**5 本指**の手袋、カフス付きの大きなブーツ。色は確定パレットのまま。

ポンチョ姿の正面シルエットを基準画像と数値で比べて、**幅のずれは平均 2.3 cm（身長 160cm に対して）**まで詰めました。
その後もう一段、基準画像を観察して詰めた（2026-09-18 後半）: 頭を丸い卵に・目を大きく・ベストを細身に（V を深く、襟を小さく、ベルトが見えるように）・腕を長く（開き 18 度）・羽の突起を見える大きさに・ブーツのカフス・裾の波・つばの反り。

### 3. リグの後にポンチョを被せる仕組み（`tools/blender/add_outfit.py`、新規）
ポンチョはテント型なので Mixamo の自動リグに通しにくい。そこで **Mixamo には素体だけを渡し、
ポンチョと帽子はリグが付いたあとに被せる**方式にしました。
帽子は Head の骨に、ポンチョは高さに応じて Hips / Spine / Spine1 / Spine2 / Neck に重みを配ります
（骨の高さはアーマチュアから読むので、Mixamo が返す大きさが変わっても付いていきます）。
そのあと既存の `add_cloth_bones.py` を流すとマントの揺れの骨が入ります。

### 4. 見比べの道具（`tools/model-view.sh` + `game/tools/model_view.tscn`、新規）
`bash tools/model-view.sh build/full res://assets/traveler_preview.glb 1.6` で .glb を四面（正面 / 左 / 背面 / 右）から描きます。

## 今どこまで動くか

- **本キャラで歩ける**（雨の景色 / ネオン / 灰色の世界すべて）
- 自動テストは**全 39 項目 OK**（`walk_test` 17（飛行の 3 項目を追加）/ `world_test` 15 / `touch_test` 7）
- `bash tools/export-web.sh` は通る（`build/web/`、約 39MB）
- `bash tools/export-web.sh` → 44MB、headless Chromium で起動確認済み
- 見た目は待機 / 歩き / 走り を四面図で確認済み（`CLIP=Walking bash tools/model-view.sh ...`）

## 不具合 3 件を直し、飛行を入れました（2026-09-18 さらに後半）

### 直したこと
1. **帽子が顔にかぶさり、目が帽子を突き抜けていた** — 原因は 2 つ
   - つばの前側を 2.4cm 下げていたため、つばの前縁（1.176m）が目の上端（1.192m）より低かった → 前下がりを 0.9cm に弱め、つばを 1.200→1.214m に上げ、襟を 1.128→1.112m に下げ、目を 1.164→1.152m に下げた
   - 頭の重みが Head と Neck に半分ずつ乗っていて、帽子（Head に 100%）とアニメ中にずれていた → **頭はまるごと Head に固く付ける**ようにした
2. **ポンチョの前が閉じていた** — 設計どおり**前開き**にした。ボタンの高さ（1.02m）より下で少しずつ開き、裾で一番広くなる。合わせ目（重ねの前身頃）の端は前あきの縁にぴったり合わせてある
3. **ジャンプで前に倒れすぎ、背中側に腕が出ていた** — Mixamo の空中クリップの前傾を **0.35 倍**に弱めた（腰と背骨の 4 本に掛ける）。腕はすでに待機ポーズに固定済み

### 飛行を入れました
- **F キー / パッド Y** で入り切り。空中を自由に飛び回れます
- 飛行中: WASD で水平移動 / **Space 上昇** / **Ctrl・C 下降** / **Shift 加速**
- 地面に触れて下降入力が無ければ自動で着地して解除
- **姿勢は「直立したまま浮かぶ」**。落下クリップ（FallingIdle）は前のめりで、とんがり帽子が前を指して魔法使いらしくなかったので、Idle をゆっくり流す形にしました。進む速さに応じて進行方向へ 20 度傾きます
- 数値はすべて F1 で調整できます（`fly_speed` 6.0 / `fly_boost_speed` 14.0 / `fly_rise_speed` 4.5 / `fly_accel` 14.0 / `fly_damp` 3.0 / `fly_lean` 20 / `fly_takeoff` 2.0 / `fly_max_height` 140）
- `fly_mode` を「飛べない」にすると F は効きません
- **飛行の専用アニメが欲しい場合の Mixamo のリスト → `docs/FLIGHT_ANIM_WISHLIST.md`**
  （最低限 **Hovering** と **Flying** の 2 本。腕が体から大きく開いていないものを選んでください）

## 本キャラをゲームに入れました（2026-09-18 後半）

**Mixamo のリグを待たずに、今のモデルで歩けるようにしました。**
既存の仮キャラのリグ（骨 28 本 + アニメ 10 本）を本キャラの寸法に合わせ直して流用しています。

### 通した工程 — `bash tools/build-character.sh` の 1 コマンド
1. メッシュを組み立てる（見た目用: 腕 18 度 / リグ用: 腕 40 度）
2. `retarget_rig.py` … 既存リグを本キャラに合わせ直す
3. `add_outfit.py` … ポンチョと帽子を被せる
4. `add_cloth_bones.py` … マントを揺らす骨 → `game/assets/traveler_cloth.glb`

ゲーム側のコードは**モデルの読み込みに関しては無改造**です（`traveler_cloth.glb` を差し替えるだけ）。

### 途中で分かった大事なこと
- **リグの合わせ方は「一様な拡大 + 上下移動」だけにすること。** 関節を本キャラの位置へ個別に動かしたら、
  右足が 6〜19cm 浮きました。Mixamo のアニメは「休めの姿勢からの回転」で入っているので、
  骨の向きや親からのずれ方を変えると手足の行き先が変わります。
  合わせたのは**股と足首の 2 点だけ**。膝・胸・首・頭は元の比率のまま少しずれますが、ポンチョと帽子に隠れます
- **足の接地判定を実測し直しました**（足音が鳴らなくなったため）。
  `godot --headless --path game res://tools/foot_probe.tscn` で測れます（新しい道具）。
  本キャラの足首は歩きで 0.193〜0.250m。判定値を 0.168/0.186 → **0.208/0.226** に更新
- **ジャンプ中も腕を待機ポーズにしました。** クリップ通りに振るとポンチョから腕が飛び出します。
  「待機・ジャンプでは腕を動かす」は前が開いたコートの頃の決定なので、ポンチョの確定デザインに合わせました。
  F1 の `arm_swing` で「クリップ通り」に戻せます

## 次の一手を 2 本用意しました（2026-09-18 前半）

今のモデルは「寸法は合っているが、絵の柔らかさ（布のシワ・たるみ）が出ていない」状態です。
そこを埋めるために、ブラウザでできる道を 2 つ用意しました。**どちらも先に進める必要はなく、
やりたいほうだけで構いません。**

### 道その1: AI 3D 生成を「参考彫刻」として 1 回だけ使う → `docs/AI_SCULPT_GUIDE.md`

- 渡す画像は用意済み: `docs/reference/character/ai_input/`（4 面図を 1 面ずつに割ったもの）
  - **まず `poncho_1_front.png` の 1 枚だけ**を Tripo AI か Meshy AI に入れるのがおすすめ
- できた .glb を `docs/reference/character/ai_sculpt/` にアップロード
- Claude 側で寸法を揃えて四面図にし、今のモデルと基準画像の 3 つを並べて、
  足りない形を `build_traveler.py` に入れ直します
- **生成物はゲームに入れません**（見比べる材料としてだけ使う）。無料枠の非商用制限に触れない使い方です

### 道その2: 自宅 PC の Blender で直接手を入れる → `docs/BLENDER_GUIDE.md`

- Blender 未経験でも通せるよう、**このプロジェクトでやることだけ**に絞った手順書を書きました
  - 画面の回し方 / モデルの開き方 / **ポンチョにシワを彫る手順** / 大きさの変え方 / 書き出し方
  - 15 分で一周できる「最初の練習」付き
- 直したら `docs/reference/character/edited/` にアップロード → Claude が四面図で見比べます
- **注意**: 手を入れるなら Mixamo に渡す前に。リグが入った後だと骨の重みが壊れます
- 手で彫ったモデルをどう扱うか（スクリプトを正とするか、彫ったものを正とするか）は
  手順書の 5 章に書いたので、そこだけ決めてください

### ついでに直したこと
- 四面図ツールの左右が基準画像と逆でした（正面 / **右** / 背面 / **左** の順に撮っていた）。
  基準画像と同じ **正面 / 左側面 / 背面 / 右側面** の順に直したので、比較画像も作り直してあります

## ユーザーにお願いしたいこと（次の一歩）

**`docs/reference/character/traveler_model_compare_poncho.png` を見て、この形で進めてよいか教えてください。**
直したいところ（帽子の反り具合、ポンチョの裾の広がり、ブーツの大きさ、目の形 など）があれば言葉で言ってもらえれば直します。

OK が出たら、**ブラウザでの作業をお願いします**（手順は `docs/MIXAMO_GUIDE.md` 0-b 章）:
1. GitHub で `docs/reference/character/traveler_body.obj` を開いて保存
2. https://www.mixamo.com/ にアップロードして自動リグ
   - マーカーの注意: **あごは首の付け根**に置く（頭が大きいので真ん中だと首が伸びます）。
     手は手袋の手のひらの中心、手首はカフス。膝は脚の真ん中より少し下
   - 腕の開きが 21 度と浅いので、Mixamo が腕を胴と誤認するかもしれません。そのときは教えてください（30 度で作り直します）
3. アニメ 10 本を FBX でダウンロード（表は MIXAMO_GUIDE 2 章。設定は前回と同じ）
4. `docs/reference/mixamo_body/` に GitHub からアップロード

そのあとは Claude が変換 → ポンチョを被せる → 揺れの骨 → Godot に差し替え、まで行います。

## 決めてほしいこと（前回からの持ち越し）

1. **ネオンの路地**をもっと増やすか、奥に何を置きたいか
2. **雨の景色の小屋**の数・大きさ・置き方、街灯を消したままでよいか
3. **マントの揺れの強さ**（F1 の `cloth_stiffness` 0.7 / `cloth_drag` 0.35 / `cloth_gravity` 0.3）
4. **身長 1.6m での歩き心地**（歩き 2.8 / 走り 6.7 / カメラ距離 3.1 が合っているか）。ワールドの小物も縮めるか
5. **スマホの Chrome でもう一度歩く**（ボタンの大きさ `touch_ui_scale`、軽さ `render_scale`）
6. **3 つ目のワールド**をどれにするか（深海 / 夕焼け / 淡い空の空間 / ビル群 / とにかく広い空間）

## 未完了・詰まっている点

- **ジャンプで膝がポンチョを突き抜ける**（脚を畳むため）。短い間ですが見えます。前あきにしたので、前から見ると開いた隙間から脚が見える形になりました
- **飛行の姿勢は Idle の流用**。専用クリップ（Hovering / Flying）を取れば本物の浮遊になります → `docs/FLIGHT_ANIM_WISHLIST.md`
- **本キャラはまだ Mixamo の本番リグではない。** 今は既存リグの流用なので、膝・胸・首・頭の骨の位置が
  本キャラの体型と少しずれています（隠れているので見た目には出ていません）
- **実機で歩いていない。** クラウドのソフトウェア描画のスクリーンショットだけで判断しています
- 基準画像 2 枚の寸法が揃っていない件は「目の帯で重ねる」で解決したが、
  **ポンチョ無しの姿は基準画像より脚が短く頭が大きく**なっています（ポンチョ姿のシルエットを優先したため）
- **足のつま先 3 本は作っていない**（ブーツに隠れて見えないため）
- 羽の突起は帽子に収まるよう短くした。基準画像より控えめです
- よじ登りのアニメは専用クリップが無く、ジャンプの脚を畳んだ姿勢を流しているだけ（仮）
- ジャンプの滞空中、Mixamo のクリップの都合で頭を下げて下を見る
- 自販機の光が近くで見ると白飛び気味。路地は 4 本とも同じ作り
- 音はすべて式で生成した仮のもの。湯気・雨どいの水には音が無い
- Windows 側の Godot / Blender のパスは未設定

## 次にやること（候補）

1. **ブラウザで飛んでみて、飛行の速さ・上昇の速さ・傾きの感想をもらう**（F1 の `fly_*` で調整）
2. 飛行の専用アニメを Mixamo で取る（`docs/FLIGHT_ANIM_WISHLIST.md` の 1 と 2）
3. 本キャラの形に OK をもらう → Mixamo で本番リグ（ユーザー）→ 差し替え（Claude）
2. タイトル画面 + ワールド選択メニュー、シンプルなセーブ
3. 3 つ目のワールド
4. ネオンの路地と雨の小屋の詰め、雨どい・湯気の音

## 自宅PCでやること（ソフト導入が要るもの）

- Godot 4.7.2 をインストールし、`game/project.godot` を開いて実機で歩く。パスを CLAUDE.md の「環境メモ」に書く
- Blender 4.x をインストールし、パスを CLAUDE.md に書く
- 内蔵 GPU でネオンのフレームレートを見る（F1 に FPS 表示あり）

## ファイルの場所

| 場所 | 内容 |
|---|---|
| `tools/blender/build_traveler.py` | **本キャラのメッシュを組み立てる**（寸法は確定画像の実測値） |
| `tools/blender/add_outfit.py` | リグ済み .glb にポンチョ + 帽子を被せて重みを付ける |
| `tools/blender/add_cloth_bones.py` | 布に揺れ用の骨の鎖を差し込む |
| `tools/blender/mixamo_fbx_to_glb.py` | Mixamo の FBX 群 → .glb（身長と材質のもと OBJ を引数で渡せる） |
| `tools/build-character.sh` | **本キャラを作ってゲームに入れるまで 1 コマンド** |
| `tools/blender/retarget_rig.py` | 既存リグを新しい素体の寸法に合わせ直す（一様な拡大 + 上下移動だけ） |
| `game/tools/foot_probe.tscn` | 足首の高さを実測する（接地判定の値を決める） |
| `docs/FLIGHT_ANIM_WISHLIST.md` | 飛行を良くするために Mixamo から取りたいクリップの一覧 |
| `tools/measure_reference.py` | 基準画像を画素で測って寸法表を出す |
| `tools/compare_silhouette.py` | 基準画像とモデルのシルエットの幅を高さごとに突き合わせる |
| `tools/split_turnaround.py` | 4 面図を 1 面ずつに割る（AI 3D 生成に渡す用） |
| `tools/blender/normalize_sculpt.py` | AI 生成メッシュを身長 1.6m・靴底 0 に揃える |
| `docs/AI_SCULPT_GUIDE.md` | AI 3D 生成を参考彫刻として使う手順 |
| `docs/BLENDER_GUIDE.md` | **Blender 初心者向けの手順書**（このプロジェクトでやることだけ） |
| `tools/model-view.sh` / `game/tools/model_view.tscn` | .glb を四面図で描く |
| `docs/reference/character/README.md` | 基準画像・パレット・**実測の寸法表**・作り方 |
| `docs/MIXAMO_GUIDE.md` | Mixamo の手順（0-b 章が本キャラ用） |
| `game/scenes/worlds/world_base.gd` | ワールド共通の土台 |
| `game/scenes/worlds/rain/` `neon/` `gray/` | 各ワールド |
| `game/scripts/tuning.gd` | 操作感・画質・風景・マントの数値（F1 で変えられる） |
| `game/scenes/player/` | 操作・カメラ・Mixamo キャラ + マントの揺れ・足音 |
| `game/assets/traveler_cloth.glb` | **今ゲームが読んでいるキャラ（仮キャラ）** |
| `game/assets/traveler_body.glb` / `traveler_outfit.glb` / `traveler_preview.glb` | 本キャラ（まだゲームでは読んでいない） |
| `game/tools/` | headless テスト（walk / world / touch） |
